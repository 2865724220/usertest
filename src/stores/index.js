import common from './common';
import redpacket from '../component/redpacket/store';
import invite from '../component/invite/store';
import inviterules from '../component/inviterules/store';

export default {
    common,
    redpacket,
    invite,
    inviterules
}